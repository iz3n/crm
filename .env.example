# Django Settings
# Copy this file to .env and update with your actual values

# You can generate one with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY='django-insecure-j0-@1ygf67y%%+)kv-)_^=dp2)glu6z2t^l09@3un0=#^00y55'
DEBUG=True
ALLOWED_HOSTS='*'

# Database Configuration
# Options: 'sqlite' or 'postgresql'
DB_ENGINE=postgresql

# PostgreSQL Database Settings (used when DB_ENGINE=postgresql)
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_CONNECT_TIMEOUT=10
DB_STATEMENT_TIMEOUT=30000

# SQLite Database Name (used when DB_ENGINE=sqlite)
# DB_NAME=db.sqlite3